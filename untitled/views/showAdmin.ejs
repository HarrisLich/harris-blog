<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link href="/css/main.css" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="/imgs/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BLOG</title>
</head>

<body class="max-w-full">
    <div class="w-full flex justify-center items-center p-4">
        <div class="w-5/6 h-fit bg-zinc-100 drop-shadow-xl rounded flex flex-col items-center">
            <h1 class="font-bold text-2xl mt-2"><%= article.title %></h1>
            <h1 class="text-gray-400"><%= article.createdAt.toLocaleDateString() %></h1>
            <div class="flex flex-row mb-2">
                <a href="/articles" class="px-2 py-1 bg-blue-500 rounded">All Articles</a>
                <form action="/<%= article.id %>?_method=DELETE" method="POST" class="inline">
                    <button type="submit"
                        class="bg-red-500 px-2 py-1 rounded ml-2">
                        Delete
                    </button>
                </form>
                <a href="/articles/edit/<%= article.id %>" class="px-2 py-1 bg-green-500 rounded ml-2">Edit</a>
            </div>
            <div class="p-4 overflow-hidden w-full h-full">
                <%- article.sanitizedHtml %>
            </div>
        </div>
    </div>
</body>

</html>